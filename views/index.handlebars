{{> nav/navbar}}

<div id="wrapper">
	<h1>Bring Order To this madness!</h1>
	<div class="row">

		<div id="articles" class="col-md-11" >
			{{#each dbGame}} {{!-- {{#if something}} --}} {{> games/game-list}} {{!-- {{/if}} --}} {{/each}}
		</div>
	</div>
</div>